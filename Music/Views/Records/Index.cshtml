@{
    Layout = "_Layout";
}
    @using Music.Models;

    <h1>Record List</h1>

    @if (Model.Count == 0)
{
            <p>There are no records in the list.</p>
}
    <ul>
    @foreach (Record record in Model)
    {
        @* <li>@record.Title</li> *@
                <li><a href='/records/@record.Id'>@record.Title</a></li>
    }
    </ul>
    <br>
    <a href="/records/delete">Clear All records</a>
    <br>
    <a href="/records/new">Add a new record.</a>